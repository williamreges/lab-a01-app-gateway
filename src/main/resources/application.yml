server:
  port: 8765

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: app-gateway
  cloud:
    config:
      .enabled: false
#    gateway:
#      discovery:
#        locator:
#          enabled: true
#          lower-case-service-id: true

